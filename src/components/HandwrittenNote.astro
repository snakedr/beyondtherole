<div class="handwritten-note">
    <p>Если этот сайт поможет спасти хотя бы одну семью, то я не зря потратил время.</p>
</div>

<style>
    .handwritten-note {
        position: absolute;
        top: 20px;
        left: 20px;
        transform: rotate(-3deg);
        font-family: 'Caveat', cursive;
        font-size: 1.2rem; /* ~20% меньше */
        color: #444; /* мягче, чем #333 */
        padding: 0; /* убрали ощущение блока */
        background: none; /* убрали подложку */
        box-shadow: none; /* без карточности */
        z-index: 999;
        max-width: 240px;
        line-height: 1.25;
        opacity: 0.85; /* эффект карандаша */
        pointer-events: none; /* не мешает интерфейсу */
    }

    @media (max-width: 768px) {
        .handwritten-note {
            top: 10px;
            left: 10px;
            font-size: 1.05rem;
            max-width: 180px;
        }
    }
</style>

