---
---
<div class="handwritten-note-container">
    <p class="note-text">Если этот сайт поможет спасти хотя бы одну семью, то я не зря потратил время</p>
</div>

<style>
   .handwritten-note-container {
        position: absolute;
        top: 25px;
        left: 20px;
        z-index: 9999; /* Максимальный приоритет */
        transform: rotate(-3deg);
        max-width: 250px;
    }

    .note-text {
        /* Добавляем !important для проверки */
        font-family: 'Dancing Script', cursive !important;
        font-size: 1.4rem;
        color: #333;
        line-height: 1.2;
        margin: 0;
        /* Убедимся, что это именно курсивный начертание */
        font-style: normal; 
    }

    /* Адаптив для мобильных */
    @media (max-width: 768px) {
        .handwritten-note-container {
            top: 10px;
            left: 10px;
            max-width: 180px;
        }
        .note-text {
            font-size: 1.1rem;
        }
    }
</style>