---
---
<div class="handwritten-note-container">
    <p class="note-text">Если этот сайт поможет спасти хотя бы одну семью, то я не зря потратил время</p>
</div>

<style>
  
      @import url('https://fonts.googleapis.com/css2?family=Marck+Script&display=swap');

    .handwritten-note-container {
        position: absolute;
        top: 25px;
        left: 25px;
        z-index: 9999;
        transform: rotate(-2deg); /* Чуть уменьшил наклон для естественности */
        max-width: 280px;
        pointer-events: none;
        user-select: none;
    }

    .note-text {
        font-family: 'Marck Script', cursive !important;
        font-size: 1.5rem;
        color: #2c2c2c; /* Сделал цвет чуть мягче черного */
        line-height: 1.1; /* Плотная склейка строк */
        margin: 0;
        /* Эффект легкого впитывания чернил в бумагу */
        opacity: 0.95;
        filter: contrast(1.1);
    }

    @media (max-width: 768px) {
        .handwritten-note-container {
            top: 15px;
            left: 15px;
            max-width: 200px;
        }
        .note-text {
            font-size: 1.2rem;
        }
    }
</style>